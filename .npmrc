# Vérifie l'intégrité des paquets (hash SHA512 vs package-lock.json)
audit=true

# Bloque l'installation si un paquet a des scripts postinstall non déclarés
ignore-scripts=false

# Refuse les versions avec des plages trop larges résolues à l'install
save-exact=false
